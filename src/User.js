// creating models
export const User = (props) => {
     return (
          <div class="text-center text-blue-700 font-medium font-sans pt-1 mt-2">
               {props.name} {props.age}
          </div>
     )
}